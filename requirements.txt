# Core
pydantic>=2.5,<3
python-dotenv>=1.0.0

# Web
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
python-multipart>=0.0.9

# Media
Pillow>=10.0
gTTS>=2.4
edge-tts>=7.0.0
requests>=2.31
deep-translator>=1.11.0   # free Google Translate fallback for multi-language subtitles

# LLM providers (all optional — pip install only what you use)
google-generativeai>=0.5; python_version>="3.9"
openai>=1.20
anthropic>=0.25

# Real video / lip-sync providers (optional — install only what you use)
fal-client>=1.0.0        # fal.ai SVD + SadTalker (set FAL_KEY in .env)
replicate>=1.0.0         # Replicate Wav2Lip + SVD (set REPLICATE_API_TOKEN in .env)

# Local Stable Diffusion (optional — set LOCAL_SD=1 to use)
# Requires CUDA-enabled torch. For RTX 30/40-series:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Then:
#   pip install diffusers transformers accelerate safetensors
diffusers>=0.27          # SDXL Turbo / SD 1.5 / Flux pipelines
transformers>=4.40
accelerate>=0.30
safetensors>=0.4

# Optional / nice-to-have
pyttsx3>=2.90        # offline TTS fallback
moviepy>=1.0.3       # not used by default; available for advanced edits
opencv-python>=4.8   # not required for default filters but enables advanced ones
numpy>=1.26          # used by some optional paths

# Test + dev
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # required by FastAPI TestClient
